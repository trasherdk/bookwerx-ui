//import {describe, it, expect, beforeEach} from 'angular2/testing'
//import {describe, it, expect, beforeEach} from 'angular2/testing'
//import {Observable} from 'rxjs/Rx'

export function main() {
  //xdescribe('Observable: basic observable', () => {
    //let n

    //setup
    //beforeEach(() => {
      //n = new Observable(observer => {
        //pushing values
        //observer.next(1)
        //observer.next(2)
        //observer.next(3)
        //complete stream
        //observer.complete()
      //})
    //})

    //specs
    //it('should create the expected sequence', done => {
      //done()
      //let expected = [1, 2, 3],
      //index = 0;
      //n.subscribe(
          //x => {                  // next
          //expect(x).toEqual(expected[index++])
          //},
          //x => results.push('#'), // error
          //x => console.log(x),
          //x => done()             // complete
          //)
          //}
      //)
   // })
  //})
}

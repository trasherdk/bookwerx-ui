//import {describe, it, expect, beforeEach} from 'angular2/testing'
//import {myCustomMatchers} from './customMatcher'

export function main() {
  /*describe("Custom matcher: toBeAllowedToDrive (TypeScript)", () => {
    let John = 17,
        Mary = 16

    // Custom Matchers must be added using beforeEach
    beforeEach(() => {
      jasmine.addMatchers(myCustomMatchers)
    })

    it("should allow John to drive", () => {
      expect(John).toBeAllowedToDrive()
      // replaces 
      expect(John).toBeGreaterThan(16)
    });

    it("should not allow Mary to drive", () => {
      expect(Mary).not.toBeAllowedToDrive()
      // replaces 
      expect(Mary).not.toBeGreaterThan(16)
    })
  })*/
}

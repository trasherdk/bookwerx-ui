describe('Edit', () => {

  beforeEach( () => {
    browser.get('/edit/1');
  });

  it('should have correct feature heading', () => {
    //expect(element(by.css('sd-about h2')).getText()).toEqual('Features');
    expect(true)
  });

});
